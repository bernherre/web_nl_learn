{
  "name": "web-nl-learn",
  "version": "19.0.1",
  "private": true,
  "type": "module",
  "description": "Statische en installeerbare Nederlandstalige leeromgeving A0-B2 met lokale profielen, toegankelijke UX, vaklexicons en een verrijkte werkwoordenatlas.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080",
    "check": "npm run build && npm run validate && npm run audit:content && node --check js/app.js && npm test",
    "build": "node scripts/build-classic.mjs",
    "validate": "node scripts/validate-project.mjs",
    "audit:content": "node scripts/audit-learning-content.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
