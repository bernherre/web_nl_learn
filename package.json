{
  "name": "web-nl-learn",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Volledig statische leeromgeving voor Nederlands met beeld, nl-NL-uitspraak, concepten en interactieve oefeningen.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080",
    "check": "node --check js/main.js && node --check js/content.js && node --check js/learning.js && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
