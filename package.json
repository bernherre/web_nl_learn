{
  "name": "web-nl-learn",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "Statische leeromgeving Nederlands A1-B2 met ruim 2.000 A1/A2-woorden en combinaties, diepe grammatica, voorzetsels, scheidbare werkwoorden, voegwoorden, beeld en nl-NL-uitspraak.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080",
    "check": "npm run build && node --check js/main.js && node --check js/content.js && node --check js/depth-content.js && node --check js/supplement-content.js && node --check js/learning.js && node --check js/app.js && npm test",
    "build": "node scripts/build-classic.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
